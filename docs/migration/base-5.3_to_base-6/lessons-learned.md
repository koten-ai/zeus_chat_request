# Lessons: base-5.3 → base-6

**Audience:** catalog authors; Client inject agents  
**Captured:** 2026-07-27 (pack content train)  
**Related:** [README.md](README.md) · [HINTS.md](../../HINTS.md) · [ROADMAP § base-6](../../ROADMAP.md)

## 1. Soft hints never replace hard law

`hints.*` is hash-excluded soft bias for **this turn**. Jailbreak keys, `company_context`, MINI-SCHEMA shape, and required Layer A stay authoritative. If a steer is true for every tenant forever, promote to CORE or `rules{}` — do not leave it as soft paste.

## 2. Multi-intent ≠ mode=open

Long multi-goal paragraphs are **planning** (decomposition + recipes + optional `hints.multipart`). Mode **open** is join/noise appetite only ([BEST_PRACTICES.md](../../BEST_PRACTICES.md)).

## 3. Size caps

Soft ~1–2 KB inject; hard-reject oversized pastes. Soft A/B paste must not thrash `contract_hash` (hash-excluded zone).

## 4. Pack can ship before Client inject

Candidate pack documents the contract. Runtime `hints.*` inject is Client residual (**ZC-WISH-040**). Do not block the pack on full product inject; do not claim production soft-steer is live until Client lands.

## 5. Snapshot folders

Train id = pack folder = `_lineage.base_id`. Never apply base-6 only via `content_train` on base-5.3.
