# base-6 overview

**Parent:** `base-5.3` · **Wire:** base-5 (objects / required four / `app_output`) — **additive content only**  
**Theme:** Soft `hints.*` inject contract + thin CORE steer note (hash-excluded Client path)  
**ROADMAP:** [docs/ROADMAP.md](../../../docs/ROADMAP.md) § base-6 · § HINTS catalog · [docs/HINTS.md](../../../docs/HINTS.md) (CR-4)

## What changed vs base-5.3

1. **Soft hints law in CORE** — optional hash-excluded `hints.*` after hard `rules{}`; never replaces jailbreak / company / MINI-SCHEMA  
2. **HINTS contract doc** — `docs/HINTS.md` (P0–P2 shapes, caps, multi-intent ≠ open)  
3. **Retained from base-5.3** — world-model CORE, verb WHEN/KEY clarity, order `field:`+`asc`, dual gaps  
4. **No Layer A wire change** — same objects / required four / `app_output`

## Non-goals

- Layer A required-field rename/remove  
- Runtime strip of hashed tools under contract enforcement  
- Pin promote (`CURRENT.json` stays base-1)  
- Zeus engine re-vendor (optional later; 0.6 may stay on base-5.3 until trial)  
- Client runtime inject implement (ZC-WISH-040 residual — pack documents the contract)

## Verify

```bash
python3 scripts/verify_base_pack.py --base 6
```
