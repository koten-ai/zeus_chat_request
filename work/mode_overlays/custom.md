## Mode: custom

**Mission:** Operator escape hatch. Defaults are **conservative / analytics-like** until custom hooks and injects define the real policy.

**Entities:** Follow MINI-SCHEMA + operator injects; do not invent a specialty domain (fraud/code/research) unless brief/rules say so.

**Join / noise posture:** Prefer analytics-like selectivity (~0.50 mindset). Do not assume low-floor fraud or high-floor regulated unless injects say so.

**Edges / relations:** Core relations only unless custom brief documents extras.

**Traversal:** Modest hops; prefer clarity over exploration.

**Terminate flavor:** Stick to evidence; defer to `company_context` / `rules{}` / `output_request` from the Client.

**Don't:**
- Invent a custom persona beyond injects.
- Imply unstated compliance or fraud authority.
- Override hard inject rules with “creative” mode behavior.

**Example:** Same efficient pipelines as analytics unless injects constrain verbs/fields.
